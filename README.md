# Time-Tracker-app
This is a simple Flutter application that helps users track the time they spend on different activities.   It uses Dart's built-in `Stopwatch` and `Timer` classes to measure elapsed time without requiring any database.
